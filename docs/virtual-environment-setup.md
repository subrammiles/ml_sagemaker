# step  A     the environment:

python -m venv .venv

This creates a folder named .venv containing a clean copy of Python.

# tep B: Activate it
You must tell your terminal to use this specific bubble.


.venv\Scripts\activate

Once activated, you should see (.venv) appear at the start of your command prompt.

#
Press Ctrl + Shift + P, type "Python: Select Interpreter", and choose the one that points to your new .venv folder.




pip install -r requirements.txt